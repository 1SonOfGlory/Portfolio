const form=document.querySelector('#idea-form');
document.querySelectorAll('[data-prompt]').forEach(button=>button.addEventListener('click',()=>{document.querySelector('#idea-topic').value=button.dataset.prompt;document.querySelector('#idea-message').focus()}));
form.addEventListener('submit',event=>{event.preventDefault();if(!form.reportValidity())return;const kind=document.querySelector('#idea-kind').value;const topic=document.querySelector('#idea-topic').value.trim();const message=document.querySelector('#idea-message').value.trim();if(message.length<10){document.querySelector('#idea-status').textContent='Please add a little more detail about your idea.';return}const name=document.querySelector('#idea-name').value.trim();const body=`Hi Jabess,\n\n${kind}: ${topic}\n\n${message}\n\n${name?`From ${name}`:''}`;const href='mailto:omanijabess47@gmail.com?subject='+encodeURIComponent('[Idea exchange] '+topic)+'&body='+encodeURIComponent(body);document.querySelector('#idea-status').textContent='Opening your email draft. Nothing is sent until you send it in your email app. If no app opens, use the email link below.';window.location.href=href});
